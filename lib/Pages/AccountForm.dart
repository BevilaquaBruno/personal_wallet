import 'package:flutter/material.dart';

class AccountForm extends StatelessWidget {
  const AccountForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Formulário de cadastro de conta"),
    );
  }
}
